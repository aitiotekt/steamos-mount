//! Protocol types re-export from core library.
//!
//! This module re-exports the daemon protocol types from the core library
//! for use in the CLI daemon implementation.

// The daemon.rs uses these types directly from core, so we don't need re-exports here.
// This module exists for potential future use.
